#############################################
# Core production requirements (pruned)
# Optional / heavy dependencies moved to:
#  - requirements-optional.txt
#  - requirements-prod.txt (alternate minimal set)
#############################################

fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-dotenv==1.0.0
email-validator==2.2.0

sqlalchemy==2.0.23
alembic==1.12.1
# Async SQLite driver
aiosqlite==0.20.0
# Async Postgres driver
asyncpg==0.29.0

spotipy==2.23.0

pydantic==2.5.0
pydantic-settings==2.1.0
requests==2.31.0
aiofiles==23.2.1
python-magic==0.4.27

# Queue and Redis client for background indexing jobs
rq==1.16.2
redis==5.0.1

# Rate limiting
slowapi==0.1.9

# NOTE: sentence-transformers / torch removed from core to cut image size.
# Install from requirements-optional.txt when enabling semantic discovery.

# End of pruned requirements
